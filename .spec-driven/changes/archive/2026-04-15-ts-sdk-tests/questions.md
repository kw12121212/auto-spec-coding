# Questions: ts-sdk-tests

## Open

<!-- No open questions -->

## Resolved

- [x] Q: Should `ts-sdk-tests` require a live Java backend, or use hermetic mocked integration tests?
  Context: This decision determines the proposal scope, delta spec wording, and testing tasks for the final remaining TypeScript SDK milestone change.
  A: Use hermetic mocked integration tests only.
