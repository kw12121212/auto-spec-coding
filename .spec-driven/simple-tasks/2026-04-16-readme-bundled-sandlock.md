## Task

Document the bundled Sandlock runtime in `README.md`.

## What was done

- Added a README section describing the bundled Sandlock runtime.
- Documented the pinned version `v0.6.0` and the default bundled binary and
  shared-library paths under `depends/sandlock/v0.6.0/linux-x86_64/`.
- Documented that `LealonePlatform.sandlock()` now defaults to the bundled copy
  and that `SPEC_DRIVEN_SANDLOCK_ENTRY` remains the explicit override.

## Spec impact

None.

## Follow-up

None.
